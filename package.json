{
  "name": "ma2-websocket-docs",
  "version": "0.0.1",
  "description": "An AsyncAPI documentation of the GrandMA 2 websockets API.",
  "scripts": {
    "buildHtml": "ag src/GrandMA2.yaml @asyncapi/html-template -o ./docs",
    "watchHtml": "watch 'npm run buildHtml' ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "grandma",
    "lighting",
    "websockets",
    "api"
  ],
  "author": "Julian Rabe",
  "license": "MIT",
  "devDependencies": {
    "@asyncapi/generator": "^1.9.14",
    "@asyncapi/html-template": "^0.7.0",
    "watch": "^1.0.2"
  }
}
